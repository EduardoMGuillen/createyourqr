DATABASE_URL="postgresql://username:password@ep-your-neon-host.neon.tech/dbname?sslmode=require"
# Production: must match the exact public URL of the site (https, no trailing slash).
# Google OAuth redirect_uri is derived from this — if it mismatches your real domain you get Error 400: redirect_uri_mismatch.
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"
# Full URL or bare host (bare host becomes https://..., localhost → http://)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google Cloud Console → Credentials → OAuth Web client → Authorized redirect URIs (add every URL you use):
#   http://localhost:3000/api/auth/callback/google
#   https://TU-DOMINIO.com/api/auth/callback/google
#   https://TU-PROYECTO.vercel.app/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# PayPal — primary trio (Vercel-style):
#   PAYPAL_API_BASE + PAYPAL_CLIENT_SECRET + NEXT_PUBLIC_PAYPAL_CLIENT_ID
# Production: PAYPAL_API_BASE="https://api-m.paypal.com"
# Sandbox:     PAYPAL_API_BASE="https://api-m.sandbox.paypal.com"
PAYPAL_API_BASE="https://api-m.sandbox.paypal.com"
PAYPAL_CLIENT_SECRET=""
NEXT_PUBLIC_PAYPAL_CLIENT_ID=""
# Optional: override default subscription plan id (P-...). If unset, the app uses the same plan
# id as the PayPal Button Factory snippet baked into src/lib/paypal-defaults.ts.
PAYPAL_PLAN_ID=""
NEXT_PUBLIC_PAYPAL_PLAN_ID=""
# PAYPAL_SUBSCRIPTION_PLAN_ID=""
# PAYPAL_BILLING_PLAN_ID=""
PAYPAL_WEBHOOK_ID=""
# Only used when PAYPAL_API_BASE is unset:
PAYPAL_ENVIRONMENT="sandbox"
# Optional legacy; otherwise the same id as NEXT_PUBLIC_PAYPAL_CLIENT_ID is used on the server.
PAYPAL_CLIENT_ID=""

# Stripe (optional: enable alongside PayPal)
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""
# You provided this product id:
STRIPE_PRO_PRODUCT_ID="prod_UO6GGPKOScHuE6"
# Optional override if you already know the exact recurring price id (price_...)
STRIPE_PRO_PRICE_ID=""

IP_HASH_SALT="replace-with-random-salt"

# Resend (welcome email after sign-up). Get API key at https://resend.com/api-keys
RESEND_API_KEY=""
# Optional. Default: CreateYourQR <onboarding@resend.dev> (ok for testing). Use your verified domain in production, e.g. CreateYourQR <hello@yourdomain.com>
RESEND_FROM=""
